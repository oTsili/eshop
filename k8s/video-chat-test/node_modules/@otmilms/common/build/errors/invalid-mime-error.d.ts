import { CustomError } from '@otmilms/common/build/errors/custom-error';
export declare class MimeValidationError extends CustomError {
    statusCode: number;
    reason: string;
    constructor();
    serializeErrors(): {
        message: string;
    }[];
}
